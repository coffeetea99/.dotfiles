# .dotfiles

```
cd
git clone https://github.com/coffeetea99/.dotfiles.git

# zsh
ln -sf .dotfiles/.zshrc

# ssh
ln -sf ../.dotfiles/.sshconfig .ssh/config

# git
ln -sf .dotfiles/.gitconfig
ln -sf .dotfiles/.gitignore_global
```
